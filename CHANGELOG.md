# 更新日志

本文件记录项目的重要变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [0.7.0] - 2026-09-02

### 新增

- 任务描述和评论支持图片以外的通用附件；普通文件会在 Agent 执行时挂载到任务 Worktree 的 `.kanban-attachments` 目录，并通过本地 Git exclude 避免进入提交。
- 详情页支持图片懒加载预览以及其他附件的文件名、类型、大小和下载入口。

### 变更

- 看板任务新建的 Agent 会话权限默认设为 Full access；恢复已有会话时保留当前权限设置。
- DSH 侧边栏改为固定显示一个跨项目的虚拟「看板任务」分组，按任务记录汇总全部 Agent 会话，不再为各任务或项目创建真实工作区。
- 插件启动时会把旧版「`{project}看板任务`」工作区中的会话迁移回任务记录，并删除这些遗留工作区，避免侧边栏持续堆积。
- 附件改为 HTTP 二进制流上传和 SHA-256 内容寻址存储，任务数据只保存轻量引用；详情页直接通过受任务归属校验的 URL 读取，不再用 Base64 RPC 搬运文件。

### 修复

- 修复 Agent 执行异常暂停后恢复会新建会话、评论续跑在恢复失败时也会静默新建会话的问题；任务现在从首次执行起绑定唯一 Agent 会话，恢复、评论和冲突处理始终续接该会话。
- 修复任务描述和评论中的图片只向 Agent 传递文件名的问题；图片现在会同时写入内容寻址存储和 DSH 附件服务，并以原生图片内容块发送给 Agent。
- 修复任务详情页未回显任务描述和评论图片附件的问题。
- 修复图片经 DSH 规范化后仍沿用原始文件格式和大小，导致 Agent 报错 `Stored attachment metadata does not match its reference`；已保存的旧任务会在再次执行时自动恢复规范引用。

## [0.6.0] - 2026-08-31

### 新增

- 从 GitHub Latest Release 检查任务看板插件的新版本，并在 DSH 全局界面弹出更新提示。
- 支持点击提示一键安装最新稳定版本；systemd 环境自动重启 `dsh-web` 并刷新页面，其他环境提示手动重启。
- 更新过程持久化安装、重启、成功和失败状态，浏览器可在 DSH 短暂重启后继续展示进度。

### 安全

- 更新器只接受仓库最新的稳定语义化版本标签，并通过无 shell 参数调用安装命令。
- `file:`、`link:` 和未知来源的安装自动禁用一键更新，避免覆盖本地开发目录或安装非预期代码。

### 测试

- 新增版本解析、安装来源识别、更新状态持久化、安装命令白名单和失败处理测试。

## [0.5.0] - 2026-08-31

### 新增

- 看板支持暗黑模式，并实时跟随 DSH 的浅色、深色和系统外观设置。
- 任务详情支持一键复制 Agent 会话 ID，并显示操作结果提示。

### 修复

- 移除看板覆盖层与 DSH 侧边栏之间的重复边框，避免暗黑模式下出现亮色竖线。
- 本地部署脚本改为复用全局 DSH 的 peer 依赖，避免依赖固定的 npx 缓存路径。

## [0.4.0] - 2026-08-31

### 新增

- 合并冲突时记录冲突文件，并允许从任务详情一键让原 Agent 在任务 worktree 中解决冲突。

### 修复

- 合并失败后自动执行 `git merge --abort` 并回写暂停状态，避免主仓库残留半合并状态、任务长期停留在“等待合回”。
- 插件重启后自动恢复处于“已审核”的未完成合并任务。
- Agent 解决冲突后校验残留冲突标记和未合并索引，提交成功后重新进入待审查。

### 测试

- 主机 smoke 测试覆盖真实分支冲突、主仓库回滚、Agent 解决冲突、重新审核、最终合并及重启后恢复未完成合并。

## [0.3.0] - 2026-08-31

### 新增

- Markdown 编辑器支持从剪贴板粘贴文件，并以包含完整路径的附件引用插入内容。
- Agent 执行完成后检查会话终态，能够识别运行时错误、中止、阻止及 token 上限等异常结果。

### 变更

- 任务详情将任务描述、执行结果与评论合并为按时间正序排列的统一任务记录。
- Agent 异常结束时将任务暂停并展示具体失败原因，避免误判为执行成功。

### 测试

- 扩展主机 smoke 测试，覆盖 Agent 运行失败后的任务暂停与错误信息记录。

## [0.2.0] - 2026-08-27

### 新增

- 引入 [agent-textarea](https://github.com/callmesoul/agent-textarea) 的 Agent Composer，用于新建任务的描述输入和任务详情的评论输入。
- 支持输入 `@` 搜索并引用当前项目文件或目录，包含键盘选择、异步加载和按项目缓存。
- 支持通过选择、拖放和剪贴板粘贴添加附件，并提供图片预览和附件移除。
- 将附件转换为现有 `file://` Markdown 引用，使主机能够把引用还原为 agent 可读的文件路径。

### 变更

- 评论框改为 Enter 提交、Shift+Enter 换行，并在输入法组合输入期间忽略提交快捷键。
- 项目目录由业务上下文自动注入，不再在任务描述和评论输入框中重复显示。
- 移除 Agent Composer 容器、文件候选浮层和附件移除按钮的阴影。
- 页面输入提示不再展示“支持 Markdown”，仅提示使用 `@` 引用项目文件。
- 将 `@vitejs/plugin-vue` 升级到 6.x，与项目使用的 Vite 6 保持兼容。
- 更新 README 与设计文档，说明 Agent Composer、文件引用、附件和键盘交互。

## [0.1.0] - 2026-08-20

### 新增

- 首次发布 DeepSeek Harness 任务看板插件。
- 提供任务状态机、git 分支调度、agent 自动执行和浏览器看板 UI。
